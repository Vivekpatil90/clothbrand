services:
  - type: web
    name: atelier-noir
    env: node
    buildCommand: npm install && npm run build
    startCommand: node .output/server/index.mjs
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
